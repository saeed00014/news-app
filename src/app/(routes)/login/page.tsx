"use client";

import { Login } from "./login";

const Page = () => {
  return (
    <section className="flex flex-col gap-2 justify-center items-center w-full h-[100vh] px-2">
      <Login />
    </section>
  );
};

export default Page;
