"use client";
import { Login } from "./login";

const Page = () => {
  return (
    <section className="flex justify-center items-center w-full h-full">
      <Login />
    </section>
  );
};

export default Page;
